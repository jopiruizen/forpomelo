To run the project do the following

clone the project:

Step 1: Install nodejs, npm, xcode and  expo cli.

Installing expo
> npm install -g expo-cli

Step 2: Install node modules 
>  npm install

Step 3: Run on ios Simulator
>  npm run ios.g
